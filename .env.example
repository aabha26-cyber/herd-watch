# =============================================================================
# HerdWatch — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your credentials.
# NEVER commit .env.local to version control.
#
# IMPORTANT: Do not place real keys in this file.

# -----------------------------------------------------------------------------
# Google Earth Engine (GEE) — Required for real satellite data
# -----------------------------------------------------------------------------
# Setup steps:
#   1. Create a GCP project: https://console.cloud.google.com
#   2. Enable Earth Engine API:
#      https://console.cloud.google.com/apis/library/earthengine.googleapis.com
#   3. Create a service account:
#      https://console.cloud.google.com/iam-admin/serviceaccounts
#   4. Register the service account for Earth Engine:
#      https://signup.earthengine.google.com/#!/service_accounts
#   5. Create/download a JSON key for the service account
#   6. Paste the FULL JSON key content below as a single line

GEE_SERVICE_ACCOUNT_KEY=
GEE_PROJECT_ID=

# -----------------------------------------------------------------------------
# ACLED API — Required for real conflict event data
# -----------------------------------------------------------------------------
# Programmatic access uses OAuth password grant (per ACLED docs):
#   - username = your ACLED account email
#   - password = your ACLED account password
# The app exchanges these for an access token at runtime.

ACLED_USERNAME=
ACLED_PASSWORD=
